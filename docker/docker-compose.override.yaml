version: "3.8"

services:
  web:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
      args:
        - ENV_FILE=./docker/.env.local
    env_file: .env.local
    # command: sleep 5000
