version: "3.8"

services:
  web:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
      args:
        - ENV_FILE=./docker/.env.local
    env_file: .env.local
    # volumes:
    #   - ../src:/app
    # command: sleep 5000
  mongodb:
    env_file: .env.local

  mongo_express:
    env_file: .env.local
